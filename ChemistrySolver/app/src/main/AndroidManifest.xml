<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chemistrysolver">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tableelement.TableElementActivity"
            android:theme="@style/Tables"></activity>
        <activity android:name=".tableelement.ElementActivity"
            android:label="Информация об элементе"></activity>
        <activity android:name=".tablesolubility.TableSolubilityActivity"
            android:label="Растворимость некоторых веществ в воде"
            android:theme="@style/Tables"></activity>
        <activity android:name=".reactionbalancing.ReactionBalancingActivity"
            android:theme="@style/Solver"></activity>
        <activity android:name=".formulas.FormulasActivity"></activity>
        <activity android:name=".tasksolver.TaskSolverActivity"
            android:theme="@style/Solver"></activity>
        <activity android:name=".tasksolver.GivenActivity"
            android:theme="@style/Solver"></activity>
        <activity android:name=".tasksolver.FindActivity"
            android:theme="@style/Solver"></activity>
        <activity android:name=".tasksolver.SolveActivity"
            android:theme="@style/Solver"></activity>
        <activity android:name=".theory.TheoryActivity"></activity>
        <activity android:name=".theory.ThemeActivity"></activity>
    </application>

</manifest>